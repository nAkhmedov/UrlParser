<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".activity.MainActivity">

    <TextView android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/first_title"/>

    <EditText android:id="@+id/firstTextView"
        style="@style/customEditText"/>

    <TextView android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/second_title"
        android:layout_below="@id/firstTextView"/>

    <EditText android:id="@+id/secondTextView" android:layout_below="@id/firstTextView"
        android:layout_marginTop="10dp"
        style="@style/customEditText"/>

    <TextView android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/third_title"
        android:layout_below="@id/secondTextView"/>

    <EditText android:id="@+id/thirdTextView" android:layout_below="@id/secondTextView"
        style="@style/customEditText"/>


    <Button android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/parse"
        android:onClick="parseDOM"/>

</RelativeLayout>
